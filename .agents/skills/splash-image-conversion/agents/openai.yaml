interface:
  display_name: "Splash Image Conversion"
  short_description: "裁剪启动图并导入 UE UI 贴图资产"
  default_prompt: "Use $splash-image-conversion to crop a source image, replace Splash files, import UE texture assets, and set Texture Group UI."
policy:
  allow_implicit_invocation: true
